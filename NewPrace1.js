// const { useCallback, useEffect } = require("react");
// const observer=useRef(null)
// const lastProdutRef=(useCallback(node)=>{
//     if(observer.curre) observer.observer.disconnecet() return false

//     observer.currenr=new IntersectionObserver((entrires)=>{
//         if(entrires[0].isIntersecting){
//             // calling api or get backed data
//             increse page 1 setPage(prev=>prev+1)
//         }
//     })
//     if(node) observer.observer(node)
    
// },[api])
// useEffect((
//     // 
// ),[page])

// const api =useCallback(()=>{

// },[fi,])

// Given a sorted array arr[] of size n, the goal is to rearrange the array so that all distinct elements appear at the beginning in sorted order. Additionally, return the length of this distinct sorted subarray.

// The duplicate elements should appear in the same order

// Example

// I => [1, 2, 2, 3, 4, 4, 4, 5, 5]

// 0 => [1, 2, 3, 4, 5, 2, 4, 4, 5]